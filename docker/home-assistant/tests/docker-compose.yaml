version: '3'
services:
  container_under_test:
    container_name: container_under_test
    image: "${IMAGE_NAME}"
    volumes:
      - ./config:/config:rw
      - /etc/localtime:/etc/localtime:ro
#    network_mode: host
    privileged: true
    ports:
      - "8123:8123"
