version: '3'
services:
  container_under_test:
    container_name: container_under_test
    image: "${IMAGE_NAME}"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    tmpfs:
      - /data
      - /config
    ports:
      - "80:80"
      - "443:443"
